# --- Node / build ---
node_modules/
dist/
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# --- Env files (keep examples) ---
.env
.env.*
!.env.example
!.env.development.example
!.env.production.example

# --- TypeScript build cache (if you enable incremental/composite later) ---
*.tsbuildinfo

# --- Coverage / test output ---
coverage/
.nyc_output/
test-results/

# --- OS / editor cruft ---
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
!.vscode/launch.json

# --- Prisma ---
# Generated prisma client ends up in node_modules/.prisma — already ignored via node_modules/
# If you ever use SQLite locally, ignore the db file:
prisma/dev.db
prisma/dev.db-journal
# Keep migrations! (Do NOT ignore prisma/migrations)
# prisma/migrations/  <-- do NOT ignore

# --- Logs & tmp ---
tmp/
*.tmp

/src/generated/prisma
